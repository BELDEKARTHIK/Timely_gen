// Stub — components now live in timetable_grid.dart
import 'package:flutter/material.dart';
class PremiumTimetableTile extends StatelessWidget {
  const PremiumTimetableTile({super.key});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}
class PeriodHeaderTile extends StatelessWidget {
  const PeriodHeaderTile({super.key});
  @override Widget build(BuildContext context) => const SizedBox.shrink();
}
