// No-op Windows stub for flutter_local_notifications.
// Prevents NuGet/MSBuild errors on Windows by replacing the
// real Windows plugin with a stub that has no C++ dependencies.

class FlutterLocalNotificationsWindowsStub {
  static void registerWith() {
    // No-op — notifications not supported on Windows in this project
  }
}
